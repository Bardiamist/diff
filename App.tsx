import {
  SafeAreaView,
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({

});

function App(): React.JSX.Element {
  return (
    <SafeAreaView>
      
    </SafeAreaView>
  );
}

export default App;
